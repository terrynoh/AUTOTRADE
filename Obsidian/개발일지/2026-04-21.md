# 개발일지 — 2026-04-21 (화요일)

> **Day 3 LIVE 실전 일지** — 삼성전기 3주 체결 + ISSUE-LIVE-08 (AES 복호화 실패) 긴급 대응 + 장 마감 후 W-LIVE-09/10 설계/배포 완료.

## 오늘 한 일

### 장중 — Day 3 LIVE 실전

**10:49:59 삼성전기 009150 매수 접수 3건 체결** (3주 @ 753,000원, 약 2,259,000원)

- order_id: `0020351800`, `0020351900`, `0020352000`
- R-13 Single→Double 전이 시점과 맞물려 매매 경로 작동
- 단, **체결통보 복호화 실패** (ISSUE-LIVE-08) 로 Watcher 내부 상태 갱신 불가
  → Trader 측만 포지션 인식, Watcher 는 "아직 매수 전" 판정

**10:49:59.0 ~ 10:49:59.22 매수 발주 8회 / 0.22초** (ISSUE-LIVE-09 발견)

- `[Coordinator] 매수 발주: 삼성전기` 로그 8회 집중
- KIS 측 접수 3건, 나머지 5건 "주문가능금액 초과" 리젝
- buy2 리젝 다수 ("초당 거래건수 초과" 포함)
- 원인: `_process_signals` 의 `await _execute_buy` 전 `_active_code` 미설정 → await 중 tick race

**10:53 수동 service stop + 원격 분석 시작**

### 장중 긴급 — M-LIVE-08-FIX

**원인 규명 시도**

- `cryptography.hazmat.primitives.ciphers` lazy import 가 systemd 런타임 + asyncio `_ws_receiver` 태스크 조합에서 `_cffi_backend` 로드 실패
- 동일 venv `python -c` 직접 실행 성공, systemd-run 재현 성공, 오직 실제 서비스 런타임 + KIS 실시간 AES 메시지 조합에서만 실패
- **근본 원인 완전 규명 실패** (재현 불가). 환경 이전(Oracle Cloud → GCP Seoul) 시 wheel ABI 미스매치 추정.

**해결 (방안 A 채택): 라이브러리 교체**

- `_decrypt_execution` 함수 단일 변경 (`src/kis_api/kis.py`)
- `cryptography.hazmat` → `pycryptodome` (`from Crypto.Cipher import AES` + `from Crypto.Util.Padding import unpad`)
- Round-trip 검증 통과 (cryptography 로 암호화한 블록을 pycryptodome 이 정확히 복호화)
- commit `96fb74a`, origin push + GCP pull + 배포 완료

### 장 마감 후 — W-LIVE-09/10 설계 + 배포

**배경**: M-LIVE-08-FIX 만으로 ISSUE-LIVE-09 재발 방지 보장 불가. 별도 방어 설계 필수 (Day 3 Terry 판단).

**W-LIVE-09 (race condition fix, P0)**

3중 방어:
1. **Coordinator `_process_signals` lock-first** (3 사이트)
   - 첫 매수 경로 (L842-851)
   - T3 예약 진입 경로 (L1301-1312)
   - T3 재판정 경로 (L1341-1350)
   - `self._active_code = chosen.code` 를 `await _execute_buy` **이전** 설정 → tick race 차단
   - except 시 active 해제 + raise
2. **`_execute_buy` idempotency guard** (L857-863): `buy1_placed=True` 시 즉시 return
3. **`Trader.place_buy_orders` 재진입 차단** (L62-74): `pending_buy_orders` 중 `is_active` 존재 시 CRITICAL 로그 + return

**W-LIVE-10 (orphan safety net, P1)**

배경: `cancel_buy_orders` 가 tracked-only 라서 8건 중 1건만 취소 시도. 나머지 7건 고아 주문.

설계 원칙 (Terry 결정):
- cancel 경로는 timing-critical → tracked-only 유지 (HTS 매매 개입 금지 정책과 유사)
- 대신 시각 기반 독립 safety net 으로 보강

구현:
1. `kis_api/constants.py`: `TR_UNFILLED = "TTTC0084R"` (구 TTTC8036R, 공식 API 문서 검증)
2. `kis_api/kis.py`: `inquire_unfilled_orders()` 메서드 (INQR_DVSN_2="2" 매수만)
3. `main.py`: F1 SA-5e 확장 — 부팅 직후 고아 미체결 감지/회수 + critical 알림
4. `watcher.py`: `on_buy_deadline` 말미 10:55 일일 safety net (루프 재발 시 마지막 차단)

**배포 완료**

- commit `474d5a7` (5 files, +153 / -4)
- origin push: ✅
- GCP pull + py_compile: ✅ (OPS_COMPILE_OK)
- 3-way HEAD 일치: 로컬 = origin = 운영 = `474d5a7`
- autotrade.service: `inactive` (장 마감 후 자연 종료, 내일 06:35 cron 자동 가동)

### 커버리지 매트릭스 (W-LIVE-10)

| 시나리오 | 기존 | W-LIVE-10 |
|---|---|---|
| 보유만 (position > 0) | F1 SA-5e | — |
| 미체결만 (position = 0) | ✗ 공백 | F1 확장 (부팅 1회) |
| 루프 재발 (주간) | ✗ 공백 | 10:55 safety net |
| 실시간 취소 | tracked-only | (유지 — timing critical) |

### Day 3 야간 — STALENESS-01 + W-N-GHOST-01 + M-N-GAP-A

**STALENESS-01 (선물 WS staleness 감시, P1)**

배경: 선물 WS 지연/끊김 시 `futures_drop_pct` 청산이 stale 가격으로 오판단. `websockets` ping_interval=30 / ping_timeout=60 자동 감지는 청산 판단에 미개입.

3중 방어:
1. `Watcher.futures_price_ts` + `_check_futures_drop(ts)`: age > 30s 시 fail-open (청산 거부)
2. `WatcherCoordinator._latest_futures_ts`: 전역 타임스탬프, 각 tick 에 전파
3. `main._futures_staleness_monitor`: 10s 폴링, 30s 초과 시 notify_error (Telegram critical), 회복 시 notify_system

구현: `src/core/watcher.py` + `src/main.py` (2 파일, `Watcher.on_tick` 시그니처 + `Coordinator.on_realtime_futures` 시그니처 확장)

**W-N-GHOST-01 (ghost 변수 제거, P2 위생)**

`main.py._subscribed_codes` 필드 0 consumer (R-15 UN 전환 잔존물). `main.py:76` + `L434` 2줄 삭제. 영향 범위 0.

**M-N-GAP-A (매도 체결 WS notification 유실 공백, P1 진단)**

semantic trace 결과:
- 매도 경로: `trader.execute_exit` → KIS → WS `H0STCNI0` → `_decrypt_execution` → `on_live_filled` → `on_sell_filled`
- 공백: WS 메시지 유실 시 `on_sell_filled` 미호출 → Watcher ENTERED 고착 → 포지션/잔고 mismatch
- 매수는 F1 SA-5e / 10:55 safety net 있으나 매도 커버 없음

Mitigation 후보 (별도 W-N 필요):
- (가) 매도 후 주기 `inquire_ccld` 폴링
- (나) `balance` 주기 조회 + ENTERED 수량 대조
- (다) `on_force_liquidate` 에서 balance 재검증 강화

상태: 미해결. 실거래 노출 중 (Day 4+ 리스크).

**CLAUDE.md vLive 2.4 업데이트**

- §3.1 라인 번호 동기화 (main.py / watcher.py / trader.py / kis.py 13 메서드)
- `inquire_unfilled_orders() L762` 추가 (LIVE-10 반영)
- §8 GAP-A 섹션 신규
- W-N-GHOST-01 실행 기록 + 변경 이력 vLive 2.4 엔트리

**배포 완료**

- commit `ba87bb4` (3 files: main.py + watcher.py + CLAUDE.md)
- origin push ✅ / GCP pull + py_compile + import ✅
- 3-way HEAD 일치: 로컬 = origin = 운영 = `ba87bb4`

**§5.6 학습 — MD5 vs git blob hash**

local MD5 ≠ GCP MD5 발견 (3 파일) → `git hash-object` normalized blob 비교로 실체 동일성 확인. 원인: Windows `core.autocrlf` 기본 동작 (local CRLF / GCP LF). 재발 방지: E5 백로그 (`.gitattributes` + `core.autocrlf`).

## 배경 / 판단

### Day 3 핵심 학습

1. **ISSUE-LIVE-08 (AES 실패) 이 ISSUE-LIVE-09 (race) 를 증폭**했을 뿐 근본 원인은 별개. 두 이슈 모두 독립적으로 해결 필요.
2. **HTS 매매 개입 금지 정책**과 유사한 사상으로 `cancel_buy_orders` 는 tracked-only 유지. 실시간 경로에 추가 API 호출 삽입 지양.
3. **시각 기반 safety net** 이 "매 tick 검사" 보다 비용 효율적. 부팅 1회 + 10:55 1회 = 일 2회 inquire 로 충분한 방어.

### Claude 실수 이력 (Day 3)

1. 초기 W-LIVE-10 설계 시 `cancel_buy_orders` 경로에 실시간 inquire 삽입 제안 → Terry 정정 (timing 손상 우려)
2. origin 3 commits 앞 선 상황을 "main.py 2250줄 재작성 충돌 확실" 로 과대 평가 → Terry 가 `diff -w = 0` 으로 CRLF/LF 이슈 확정 (§5.6 학습 적용 성공 사례 = 수석님 쪽)

## 내일 (Day 4, 2026-04-22 수요일)

### 실전 검증 포인트

**M-LIVE-08-FIX**:
- 체결통보 `H0STCNI0` 수신 시 AES 복호화 성공 + 26 필드 파싱 성공
- `trader.on_live_acknowledged/filled` 경로 정상 진입
- Phase 2 B Position dual-write 불일치 경고 0건

**W-LIVE-09 (race fix)**:
- `[Coordinator] 매수 발주: {code}` 로그 1종목당 1회만 출력
- `[Trader] CRITICAL: place_buy_orders 재진입 차단` 로그 0건 (방어선 자체가 작동하면 1/2차 보완 필요)

**W-LIVE-10 (orphan safety net)**:
- 부팅 로그 `[F1 확장] ... 미체결 매수 ...건 감지` — 0건 = 정상
- 10:55 로그 `[Coordinator] 10:55 safety net:` — 0건 = 정상

**STALENESS-01 (선물 WS)**:
- `[선물 staleness]` 로그 0건 = 정상 (발생 시 Telegram critical + 30s 내 회복 로그)
- Watcher 청산 판단 시 `futures_price_ts` age 검증 경로 작동

### 보류 / 다음 세션

- **GAP-A (P1)**: 매도 체결 WS notification 유실 공백 해소 (별도 W-N 필요, 실거래 노출 중 → Mitigation 3갈래 후보 결정 필요)
- **E8**: trades.db 에 Day 3 삼성전기 3주 체결 수기 INSERT (AES 실패로 미기록)
- **E6**: requirements.txt 에서 `cryptography` 제거
- **E5**: `.gitattributes` 추가 + `core.autocrlf` 설정 (CRLF/LF 충돌 방지 — ba87bb4 검증 시 MD5 mismatch 로 재발견)
- **E7**: `docs/OPS_ENVIRONMENT_MIGRATION_CHECKLIST.md` 작성

## 관련 문서

- [[이슈_트래커]] — [LIVE-08] / [LIVE-09] / [LIVE-10] / [STALENESS-01] / [GAP-A] 기록
- CLAUDE.md — vLive 2.4 버전 반영 (§3.1 라인 동기화 + §8 GAP-A)
- commit `96fb74a` (M-LIVE-08-FIX)
- commit `474d5a7` (W-LIVE-09/10)
- commit `ba87bb4` (STALENESS-01 + W-N-GHOST-01 + CLAUDE.md vLive 2.4)
