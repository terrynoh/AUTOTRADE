# 2026-04-10 — B-01 백테스트 방법론: 단순 시뮬레이터 (옵션 F)

## 결정
B-01 백테스트는 *운영 코드 import* 방식 (옵션 D/E) 을 폐기하고 **단순 시뮬레이터** (옵션 F) 로 진행. yaml 직접 로드 + CLAUDE.md §2 명세 1:1 매핑.

## 근거 (사실 base)
1. **본 작업 목적 = 분석 + 리포팅** ("오늘 입력 62 종목이 R-08 명세대로 처리됐다면 어떤 결과가 나왔을지 시뮬레이션")
2. 운영 코드 import 방식의 누적 비용:
   - WatcherCoordinator/Trader 시그니처 인벤토리 5건 필요
   - KISAPI Mock + DRY_RUN 분기 추적
   - async/sync 정합 추적
   - **wall-clock 가드 발견** (`trader.py:43` `_now.time() >= 11:00` → 발주 거부) → 4/11 새벽 안전 윈도우 대기 필요
   - setup_logger 격리, 토큰 캐시 격리
3. 단순 시뮬레이터의 정합성 가드:
   - yaml 직접 로드 → 16 동결값 비트 단위 동일
   - CLAUDE.md §2 명세 표 ↔ analyze.py 코드 1:1 매핑
   - 호가 단위 4 함수 직접 구현 (운영 `price_utils.py` 와 동일 알고리즘)
   - 리포트에 *별도 구현* 명시

## 대안 (검토했으나 채택 안 함)
- **옵션 D — Trader 서브클래스 + place_buy_orders 오버라이드**: 본체 복사 = "재구현 0건" 위반
- **옵션 E — 운영 코드 import + 4/11 새벽 실행**: 가능하지만 12 시간 대기 + 통합 테스트 영역 침범 (목적과 불일치)
- **옵션 A/B/C — 시장 구분 추측 매핑**: §5.6 사실 base 위반

## 단순화 항목 (의식적, 리포트 명시)
| 항목 | 운영 | 시뮬 |
|---|---|---|
| 매수 분할 | buy1 50% + buy2 50% | buy1 체결 = full position |
| T2 시점 | buy2 체결 | T1 ENTERED 시점 |
| tick | 실시간 ws | close only 분봉 |
| 선물 청산 | -1% 발화 | 데이터 부재 → 비활성 |

## 영향 영역
- **코드**: backtest/B-01_2026-04-10/ (신규 디렉토리, src/ 0줄 수정)
- **명세**: 영향 없음 (yaml read-only)
- **운영**: 영향 없음 (서버 0건 접근)
- **vault**: 본 결정 + 개발일지 + 이슈 트래커 3건

## 후속 작업
- 통합 검증 필요 시 옵션 E (운영 코드 import 트랙) 별도 트랙으로 재기동 가능 (현재 미필요 판단)
- analyze.py v2 (buy1+buy2 분할, OHLC 4-tick) 는 4/13 첫 거래일 검증 후 별도 작업

## 결과
- 매매 1건 (005290 -2.75%, -1,350원, hard_stop)
- TRIGGERED 48/62
- 누적 P&L -0.003%, daily_loss_limit 안전망 안
- R-08 보수적 매매 철학 그대로 작동 확인
