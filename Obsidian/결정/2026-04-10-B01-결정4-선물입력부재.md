# 결정 4 — B-01 선물 분봉 입력 부재

> **날짜**: 2026-04-10  
> **컨텍스트**: B-01 백테스트 (2026-04-10 장중 replay)  
> **관련 Phase**: Phase 1.B (수집) → Phase 2 (replay harness)

---

## 결정 (한 줄)

B-01 replay 에서 KOSPI200 선물 분봉 입력 없이 4 청산 조건만 사용한다.

---

## 근거 (사실 base)

- KIS API `FHKST03010200` 에 `FID_COND_MRKT_DIV_CODE="F"` (선물) 를 넘겼으나 빈 응답 반환
- 101S3000 (KOSPI200 선물 근월물) 분봉 수집 결과: 0건
- 재시도 방법 없음 — 당일 장 마감 후, 해당 시장의 분봉 API 엔드포인트 미지원 가능성 높음
- 수집 스크립트 자체적으로 선물 0건 시 Phase 2 비활성 처리 권고 출력함

---

## 대안 (검토했으나 채택 안 한 길)

| 대안 | 이유 (채택 X) |
|---|---|
| 다른 TR ID 탐색 (선물 전용) | 공식 레퍼런스 부재, 당일 재시도 불가 |
| 선물 데이터 외부 소스 확보 | 시간/리소스 부족, B-01 범위 초과 |
| 선물 가격 고정값 주입 (추정) | 사실 base 위반, 결과 오염 |

---

## 영향 영역

| 영역 | 영향 |
|---|---|
| **운영 코드** | **무수정** — `src/core/watcher.py`, `config/strategy_params.yaml` 변경 없음 |
| **replay harness** | `futures_price` 를 None 또는 고정값으로 주입 → `_check_futures_drop` 미발동 |
| **결과 해석** | 선물 급락 조건 발동 케이스는 이번 백테스트에서 관측 불가 |
| **운영 전략** | 4/13 실거래 시 선물 조건은 정상 동작 (운영 코드 무수정이므로) |

**원복 대상 없음. 운영 코드 무수정 유지.**

---

## 후속 작업

- Phase 2 replay 명령서에 의무 명시:
  - `futures_price` 입력 = `None` 고정 또는 별도 주의 표기
  - replay 종료 후 `git diff --stat config/strategy_params.yaml src/core/watcher.py src/core/trader.py` → 모두 빈 결과 필수
  - 한 줄이라도 diff 나오면 즉시 멈춤 + 사고 보고
- B-01 결과 리포트에 "선물 조건 미검증" 주석 명시

---

## 운영 토큰 영향 평가

Phase 1.B 수집 중 로컬 `token_live.json` 재발급 가능성 있음 (4/7 만료 예상).

| 항목 | 평가 |
|---|---|
| 4/11 (토) ~ 4/12 (일) | 비거래일, 운영 서버 KIS 호출 거의 0 |
| 4/13 (월) 06:40 cron | 어차피 자연 만료 → 자동 재발급 로직이 새로 발급 |
| **결론** | **영향 영역 거의 없음** |

**4/13 가동 시 로그 확인 필요**: `token re-issued` 또는 `token loaded from cache` 메시지 확인.
