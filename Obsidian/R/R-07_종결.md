# R-07 종결

> 🟢 **활성 영역 / 영구 보존** — R-07 의 결정 + 변경 + 사고 영구 기록.
>
> **종결일**: 2026-04-09
> **W-16 정합**: 2026-04-10 (사후 정리)
> **단일 진실 참조**: `CLAUDE.md` §11 (이력 + 사고 이력)

---

## R-07 의 정체

Watcher + WatcherCoordinator 중심의 *상태 기반 매매 엔진* 재설계. R-04 (단순 monitor 기반) 의 한계 — 단일 monitor + _monitors 리스트 stale 사고 (ISSUE-036/037) 를 근본 해소하기 위해 도입.

---

## 핵심 변경

### 아키텍처
- `monitor.py` → `watcher.py` 로 전면 재작성
- 단일 `Monitor` → `Watcher` (8 state machine) + `WatcherCoordinator` (N 병렬 관리)
- 상태 기반 전이: WATCHING → TRIGGERED → READY → ENTERED → EXITED + SKIPPED / DROPPED / PASSED
- single active rotation 패턴 (동시 1 종목만 active, 나머지는 대기)

### 매매 명세 (R-04 → R-07)
- 09:50 / 10:55 / 11:20 시각 트리거
- 매수 비율: KOSPI -2.7%/-3.3%, KOSDAQ -4.0%/-5.0% (R-08 W-14 에서 변경)
- 손절: KOSPI -4.0%, KOSDAQ -5.0% (R-08 에서 -4.1%/-6.15% 로 변경)

### 명세 정정
- W-09: dashboard ISSUE-035 정정 (set-targets / run-manual-screening 의 KIS API 컨텍스트 충돌 해소, run_coroutine_threadsafe 위임 패턴)

---

## R-07 종결 시점 평가 (2026-04-09)

매매 0건 baseline day 통과 — 시장 조건 미충족 = 정상 동작 확인. Watcher 8 state 전이 정상.

---

## R-08 진행 중 R-07 재평가 (2026-04-09)

R-07 은 "종결" 이 아니라 "운 좋게 테스트되지 않았던 미완성 상태" 였음. 5건 빈틈 발견:

1. **두 번째 매매 예약 구조 부재** — `multi_trade.repeat_start/end` yaml 정의 / 코드 0 → R-08 W-11d/e 해소
2. **setup_logger() 미호출** — 파일 로그 0 → R-08 W-10b 해소
3. **dashboard `.stock` 잔존 참조** — W-07a 마이그레이션 후 잔존 → R-08 백로그 #3 (현재 미해결)
4. **ISSUE-E1 rapid restart transient** — 토큰 캐시 read-only 일시 사고 → 방어 설계로 흡수
5. **multi_trade.repeat_start/end 미구현** — yaml 만 있고 코드 0 → R-08 W-11d/e 해소

---

## 학습

- "완료" ≠ 완료 — 라이브 조건으로 검증되지 않은 기능은 *잠재적 미완성*
- yaml ≠ 구현 — yaml 키와 코드 참조는 별도 검증 필요
- single active rotation 만으로는 부족 — N차 매매 연쇄 명세를 위한 reservation 구조 필요
- 좁은 진단 (M-N 1차) 은 빈틈을 놓침 — full inventory M-N 권장

---

## R-07 시점 코드 라인 수 (참고)

| 파일 | 라인 |
|---|---|
| `src/core/watcher.py` | 768 (R-08 후 1190, +422) |
| `src/main.py` | ~975 (R-08 후 ~980) |

---

## 관련 영역

- **다음 R**: [[R/R-08_종결]]
- **이전 영역**: R-04 동결본은 `_archive/2026-04-pre-R08/01_전략_마스터스펙_stale.md`
- **이슈 트래커**: [[이슈_트래커]] (R-08 backlog 4 건이 R-07 잔존 영역)
